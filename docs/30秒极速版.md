# ⚡ 30秒极速版

> 给时间紧迫、只想快速验证的用户

## 一句话版本

**复制粘贴这个命令，30秒内验证所有服务：**

```bash
curl -fsSL https://clawdrepublic.cn/probe-roc-all.sh | bash
```

## 三句话版本

1. **测试网络**：`curl -fsS -m 5 https://clawdrepublic.cn/ > /dev/null && echo "✅ 网络正常"`
2. **测试API**：`curl -fsS -m 5 https://api.clawdrepublic.cn/healthz`
3. **申请密钥**：访问 https://clawdrepublic.cn/forum/ 发帖申请

## 完整验证脚本

如果你想要更详细的验证结果，用这个：

```bash
#!/bin/bash
echo "🔍 Clawd国度服务验证脚本"
echo "=========================="

# 1. 网站可访问性
echo -n "🌐 网站访问... "
if curl -fsS -m 5 https://clawdrepublic.cn/ > /dev/null; then
    echo "✅ 正常"
else
    echo "❌ 失败"
fi

# 2. API健康检查
echo -n "⚙️ API健康... "
if curl -fsS -m 5 https://api.clawdrepublic.cn/healthz 2>/dev/null | grep -q '"ok":true'; then
    echo "✅ 正常"
else
    echo "❌ 失败"
fi

# 3. 模型列表
echo -n "🤖 模型列表... "
if curl -fsS -m 5 https://api.clawdrepublic.cn/v1/models 2>/dev/null | grep -q '"object":"list"'; then
    echo "✅ 正常"
else
    echo "❌ 失败"
fi

# 4. 配额代理
echo -n "🔑 配额代理... "
if curl -fsS -m 5 https://api.clawdrepublic.cn/quota-proxy/healthz 2>/dev/null | grep -q '"ok":true'; then
    echo "✅ 正常"
else
    echo "❌ 失败"
fi

echo "=========================="
echo "🎉 验证完成！如需试用密钥，请访问："
echo "👉 https://clawdrepublic.cn/forum/"
```

## 下一步

验证通过后：
1. 访问论坛申请试用密钥
2. 查看[极小白教程](./极小白_零基础_一条龙.md)进行完整配置
3. 开始你的AI项目！

## 问题反馈

遇到问题？请到论坛发帖，我们会尽快回复：
- 论坛：https://clawdrepublic.cn/forum/
- 问题求助板块：https://clawdrepublic.cn/forum/category/2